import { CartItemForUpdate } from './cart-item-for-update';

describe('CartItemForUpdate', () => {
  it('should create an instance', () => {
    expect(new CartItemForUpdate()).toBeTruthy();
  });
});
