import { CouponForCartUpdate } from './coupon-for-cart-update';

describe('CouponForCartUpdate', () => {
  it('should create an instance', () => {
    expect(new CouponForCartUpdate()).toBeTruthy();
  });
});
