import { ProductForCartItemUpdate } from './product-for-cart-item-update';

describe('ProductForCartItemUpdate', () => {
  it('should create an instance', () => {
    expect(new ProductForCartItemUpdate()).toBeTruthy();
  });
});
